---
import NavButton from "./NavButton.svelte";

---

<div class="flex flex-row lg:ml-8 items-center flex-wrap gap-4 p-2 rounded-sm shadow bg-oxford-800 bg-opacity-60 backdrop-blur-sm">
    <NavButton title="about" url="/" path={Astro.url.pathname} client:load /> |
    <NavButton title="projects" path={Astro.url.pathname} client:load />
    <!-- <NavButton title="design" path={Astro.url.pathname} client:load /> | -->
    <!-- <NavButton title="blog" path={Astro.url.pathname} client:load /> -->
</div>
